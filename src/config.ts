interface Config {
    url:string,
    timeout: number,
    projectName:string
}

export const config:Config = {
    url:"https://hesamsrk.github.io/starwars-react-demo/",
    timeout: 20000,
    projectName:"react-demo"
}